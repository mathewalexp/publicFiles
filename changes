val xmlFilePath = "dbfs:/path/to/your/file.xml"

// Read the XML file into a DataFrame
val df = spark.read
  .format("xml")
  .option("rowTag", "yourRowTag") // Replace 'yourRowTag' with the actual row tag
  .load(xmlFilePath)
