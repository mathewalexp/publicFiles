 int count = 1;
            String newFileName;
            File newFile;

            do {
                newFileName = originalFileName.replaceFirst("\\.([^.]*)$", "(" + count + ").$1"); // Append (count) before the extension
                newFile = new File(folderPath, newFileName);
                count++;
            } while (newFile.exists());
